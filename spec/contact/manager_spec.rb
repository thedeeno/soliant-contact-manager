require 'spec_helper'

describe Contact::Manager do
  it 'has a version number' do
    expect(Contact::Manager::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
